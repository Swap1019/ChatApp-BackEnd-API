# GIFs

Status: domain model and DbContext mapping implemented.

The current backend includes `GifMetadata` and `UserRecentGif` with EF Core
configuration. GIF search, trending, recent, and upload endpoints are planned.
